{
  "name": "shopping-catalog",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "bot:dev": "npm run dev --workspace=@catalog/bot",
    "site:dev": "python3 -m http.server 8000 --directory docs"
  }
}
